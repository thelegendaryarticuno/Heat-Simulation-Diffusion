The python programme uses library: Numpy and Matplotlib 
Uses the tool: FFmpeg and moviemaker

It illustrates:

Simulation and diffusion of heat using colour code from very high temperature (Red) to cooler temperature (Blue).
